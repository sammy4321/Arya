import 'package:arya_app/src/features/settings/pages/placeholder_settings_page.dart';
import 'package:flutter/widgets.dart';

class PaymentVaultSettingsPage extends StatelessWidget {
  const PaymentVaultSettingsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const PlaceholderSettingsPage(
      message: 'Payment vault page is ready for billing and card details.',
    );
  }
}
