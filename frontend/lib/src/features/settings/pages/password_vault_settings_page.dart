import 'package:arya_app/src/features/settings/pages/placeholder_settings_page.dart';
import 'package:flutter/widgets.dart';

class PasswordVaultSettingsPage extends StatelessWidget {
  const PasswordVaultSettingsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const PlaceholderSettingsPage(
      message: 'Password vault page is ready for secure credential fields.',
    );
  }
}
